import _ from 'lodash';
import { createStore } from 'vuex';

const store = createStore({
	devtools: true,
	namespaced: true,
	state: {},
	actions: {},
	getters: {},
	mutations: {},
});

export default store;
